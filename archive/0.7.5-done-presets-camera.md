
presets: save to js, load default.js at startup, fill dropdown preset list (above save load buttons), presets_combine.py search all presets in CWD and add them to default.js get preset name from filename, then rename added to *.delete-me.

save button save 
2d/3d
is tails draw checked
is whole
as
2d-yy-mm-dd-hh-mm-ss.js
3d-tails-whole-yy-mm-dd-hh-mm-ss.js


auto-camera add 2 sliders to choose speed. log scale. as we have several log scale sliders, better deduplicate code.